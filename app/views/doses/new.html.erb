<h1>Add some doses to your crazy cocktail!</h1>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Add it", class: "btn btn-primary" %>
<% end %>
